// set_sprite( array(spr_pair, spr_pair, spr_pair) ) 
// Set an object's sprite index and speed from a sprite array created by sprite()
// FOR EACH element in the array

var spr_array = argument0;

var spr_pair = argument0;

sprite_index = spr_array[0];
image_speed = spr_array[1];
